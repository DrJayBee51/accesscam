"""Entry point: `python -m accesscam` or the `accesscam` console script."""

from accesscam import __version__


def main() -> None:
    # Placeholder until Milestone 1 (camera bring-up) lands.
    print(f"AccessCam {__version__} — nothing to run yet. See docs/PROJECT_PLAN.md.")


if __name__ == "__main__":
    main()
